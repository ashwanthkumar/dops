package server

// Server is the D-Ops Registry server which acts as a local cache registry and also seeds
// the images for a specified duration
type Server struct{}

// Start - Starts the Server
func (s *Server) Start() {
}

// Stop - Stops the server (for clean shutdown)
func (s *Server) Stop() {
}
